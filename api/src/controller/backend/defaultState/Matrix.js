const { Modes } = require('./matrixModes')

let state = []

for (const key in Modes) {
  this.state.push({
    info: Modes[key].info,
    config: mode.getWholeConfig(),
    alpha: 0
  })
}

module.exports = [

]